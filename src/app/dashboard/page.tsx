'use client';

import Head from 'next/head';

const page = () => {
  return (
    <div>
      <Head>
        <title>Dashboard</title>
      </Head>
      <h1>Dashboard</h1>
    </div>
  );
};

export default page;
