// Use this folder to add your context provider files
// then in the hooks folder you can add your custom hooks to use them in your components/pages

// for example:
// context/UserContext.tsx -> saves information about the current user
// hooks/useUser.tsx -> provides an abstraction of UserContext to get the current user

export {};
